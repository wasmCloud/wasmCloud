package wasmcloud:http-blobstore-service;

// wasi:http/incoming-handler is exported via wstd-axum's #[http_server] proc macro,
// so it does not appear explicitly in this world definition.
world http-blobstore-service {
  import wasi:blobstore/blobstore@0.2.0-draft;
}
