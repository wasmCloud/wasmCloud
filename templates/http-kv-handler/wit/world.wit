package wasmcloud:http-kv-handler;

// Versions here are pinned by `wkg.lock`; bindings are generated by
// `wit-bindgen` (pinned in Cargo.toml). Bump together if you need a
// newer `wasi-http` patch.
world http-kv-handler {
  import wasi:keyvalue/store@0.2.0-draft;

  export wasi:http/incoming-handler@0.2.2;
}
