[package]
name = "http-kv-handler"
edition = "2024"
version = "0.1.0"

[workspace]

[lib]
crate-type = ["cdylib"]

[dependencies]
serde = { version = "1.0.217", features = ["derive"] }
serde_json = "1.0"
wit-bindgen = "0.41.0"

[profile.release]
lto = true
opt-level = "s"
strip = true
