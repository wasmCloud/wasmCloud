[package]
name = "test-virtual-components"
version = "0.1.0"
edition = "2021"
publish = false
license = "Apache-2.0"

[features]
docs = []

[package.metadata.docs.rs]
features = ["docs"]

[build-dependencies]
anyhow = { workspace = true, features = ["std"] }
tokio = { workspace = true, features = [
    "fs",
    "macros",
    "process",
    "rt-multi-thread",
] }
