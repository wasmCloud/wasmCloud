[package]
name = "wrpc-pinger-component"
edition = "2021"
description = """
A wit-based actor that invokes other components (the ponger) to demonstrate wRPC invocations.
"""
version = "0.1.0"

[lib]
crate-type = ["cdylib"]

[dependencies]
wit-bindgen = { workspace = true }
