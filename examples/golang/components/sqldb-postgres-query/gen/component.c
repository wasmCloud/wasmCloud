// Generated by `wit-bindgen` 0.33.0. DO NOT EDIT!
#include "component.h"
#include <stdlib.h>
#include <string.h>

// Imported Functions from `wasmcloud:postgres/query@0.1.1-draft`

__attribute__((__import_module__("wasmcloud:postgres/query@0.1.1-draft"), __import_name__("query")))
extern void __wasm_import_wasmcloud_postgres_query_query(uint8_t *, size_t, uint8_t *, size_t, uint8_t *);

__attribute__((__import_module__("wasmcloud:postgres/query@0.1.1-draft"), __import_name__("query-batch")))
extern void __wasm_import_wasmcloud_postgres_query_query_batch(uint8_t *, size_t, uint8_t *);

// Exported Functions from `wasmcloud:examples/invoke`

__attribute__((__weak__, __export_name__("cabi_post_wasmcloud:examples/invoke#call")))
void __wasm_export_exports_wasmcloud_examples_invoke_call_post_return(uint8_t * arg0) {
  if ((*((size_t*) (arg0 + 4))) > 0) {
    free(*((uint8_t **) (arg0 + 0)));
  }
}

// Canonical ABI intrinsics

__attribute__((__weak__, __export_name__("cabi_realloc")))
void *cabi_realloc(void *ptr, size_t old_size, size_t align, size_t new_size) {
  (void) old_size;
  if (new_size == 0) return (void*) align;
  void *ret = realloc(ptr, new_size);
  if (!ret) abort();
  return ret;
}

// Helper Functions

void wasmcloud_postgres_types_query_error_free(wasmcloud_postgres_types_query_error_t *ptr) {
  switch ((int32_t) ptr->tag) {
    case 0: {
      component_string_free(&ptr->val.invalid_params);
      break;
    }
    case 1: {
      component_string_free(&ptr->val.invalid_query);
      break;
    }
    case 2: {
      component_string_free(&ptr->val.unexpected);
      break;
    }
  }
}

void wasmcloud_postgres_types_statement_prepare_error_free(wasmcloud_postgres_types_statement_prepare_error_t *ptr) {
  switch ((int32_t) ptr->tag) {
    case 0: {
      component_string_free(&ptr->val.unexpected);
      break;
    }
  }
}

void wasmcloud_postgres_types_prepared_statement_exec_error_free(wasmcloud_postgres_types_prepared_statement_exec_error_t *ptr) {
  switch ((int32_t) ptr->tag) {
    case 1: {
      wasmcloud_postgres_types_query_error_free(&ptr->val.query_error);
      break;
    }
    case 2: {
      component_string_free(&ptr->val.unexpected);
      break;
    }
  }
}

void wasmcloud_postgres_types_ipv4_addr_free(wasmcloud_postgres_types_ipv4_addr_t *ptr) {
  component_string_free(ptr);
}

void wasmcloud_postgres_types_ipv6_addr_free(wasmcloud_postgres_types_ipv6_addr_t *ptr) {
  component_string_free(ptr);
}

void wasmcloud_postgres_types_subnet_free(wasmcloud_postgres_types_subnet_t *ptr) {
  component_string_free(ptr);
}

void wasmcloud_postgres_types_xip_list_free(wasmcloud_postgres_types_xip_list_t *ptr) {
  size_t list_len = ptr->len;
  if (list_len > 0) {
    int64_t *list_ptr = ptr->ptr;
    for (size_t i = 0; i < list_len; i++) {
    }
    free(list_ptr);
  }
}

void wasmcloud_postgres_types_column_name_free(wasmcloud_postgres_types_column_name_t *ptr) {
  component_string_free(ptr);
}

void wasmcloud_postgres_types_numeric_free(wasmcloud_postgres_types_numeric_t *ptr) {
  component_string_free(ptr);
}

void component_option_u16_free(component_option_u16_t *ptr) {
  if (ptr->is_some) {
  }
}

void wasmcloud_postgres_types_option_lexeme_weight_free(wasmcloud_postgres_types_option_lexeme_weight_t *ptr) {
  if (ptr->is_some) {
  }
}

void wasmcloud_postgres_types_lexeme_free(wasmcloud_postgres_types_lexeme_t *ptr) {
  component_option_u16_free(&ptr->position);
  wasmcloud_postgres_types_option_lexeme_weight_free(&ptr->weight);
  component_string_free(&ptr->data);
}

void wasmcloud_postgres_types_offset_free(wasmcloud_postgres_types_offset_t *ptr) {
  switch ((int32_t) ptr->tag) {
    case 0: {
      break;
    }
    case 1: {
      break;
    }
  }
}

void wasmcloud_postgres_types_date_free(wasmcloud_postgres_types_date_t *ptr) {
  switch ((int32_t) ptr->tag) {
    case 2: {
      break;
    }
  }
}

void wasmcloud_postgres_types_interval_free(wasmcloud_postgres_types_interval_t *ptr) {
  wasmcloud_postgres_types_date_free(&ptr->start);
  wasmcloud_postgres_types_date_free(&ptr->end);
}

void wasmcloud_postgres_types_time_tz_free(wasmcloud_postgres_types_time_tz_t *ptr) {
  component_string_free(&ptr->timesonze);
}

void wasmcloud_postgres_types_timestamp_free(wasmcloud_postgres_types_timestamp_t *ptr) {
  wasmcloud_postgres_types_date_free(&ptr->date);
}

void wasmcloud_postgres_types_timestamp_tz_free(wasmcloud_postgres_types_timestamp_tz_t *ptr) {
  wasmcloud_postgres_types_timestamp_free(&ptr->timestamp);
  wasmcloud_postgres_types_offset_free(&ptr->offset);
}

void component_list_s64_free(component_list_s64_t *ptr) {
  size_t list_len = ptr->len;
  if (list_len > 0) {
    int64_t *list_ptr = ptr->ptr;
    for (size_t i = 0; i < list_len; i++) {
    }
    free(list_ptr);
  }
}

void component_list_bool_free(component_list_bool_t *ptr) {
  size_t list_len = ptr->len;
  if (list_len > 0) {
    bool *list_ptr = ptr->ptr;
    for (size_t i = 0; i < list_len; i++) {
    }
    free(list_ptr);
  }
}

void component_list_hashable_f64_free(component_list_hashable_f64_t *ptr) {
  size_t list_len = ptr->len;
  if (list_len > 0) {
    wasmcloud_postgres_types_hashable_f64_t *list_ptr = ptr->ptr;
    for (size_t i = 0; i < list_len; i++) {
    }
    free(list_ptr);
  }
}

void component_list_hashable_f32_free(component_list_hashable_f32_t *ptr) {
  size_t list_len = ptr->len;
  if (list_len > 0) {
    wasmcloud_postgres_types_hashable_f32_t *list_ptr = ptr->ptr;
    for (size_t i = 0; i < list_len; i++) {
    }
    free(list_ptr);
  }
}

void component_list_s32_free(component_list_s32_t *ptr) {
  size_t list_len = ptr->len;
  if (list_len > 0) {
    int32_t *list_ptr = ptr->ptr;
    for (size_t i = 0; i < list_len; i++) {
    }
    free(list_ptr);
  }
}

void component_list_numeric_free(component_list_numeric_t *ptr) {
  size_t list_len = ptr->len;
  if (list_len > 0) {
    wasmcloud_postgres_types_numeric_t *list_ptr = ptr->ptr;
    for (size_t i = 0; i < list_len; i++) {
      wasmcloud_postgres_types_numeric_free(&list_ptr[i]);
    }
    free(list_ptr);
  }
}

void component_list_s16_free(component_list_s16_t *ptr) {
  size_t list_len = ptr->len;
  if (list_len > 0) {
    int16_t *list_ptr = ptr->ptr;
    for (size_t i = 0; i < list_len; i++) {
    }
    free(list_ptr);
  }
}

void component_list_list_s16_free(component_list_list_s16_t *ptr) {
  size_t list_len = ptr->len;
  if (list_len > 0) {
    component_list_s16_t *list_ptr = ptr->ptr;
    for (size_t i = 0; i < list_len; i++) {
      component_list_s16_free(&list_ptr[i]);
    }
    free(list_ptr);
  }
}

void component_list_u8_free(component_list_u8_t *ptr) {
  size_t list_len = ptr->len;
  if (list_len > 0) {
    uint8_t *list_ptr = ptr->ptr;
    for (size_t i = 0; i < list_len; i++) {
    }
    free(list_ptr);
  }
}

void component_tuple2_u32_list_u8_free(component_tuple2_u32_list_u8_t *ptr) {
  component_list_u8_free(&ptr->f1);
}

void component_list_tuple2_u32_list_u8_free(component_list_tuple2_u32_list_u8_t *ptr) {
  size_t list_len = ptr->len;
  if (list_len > 0) {
    component_tuple2_u32_list_u8_t *list_ptr = ptr->ptr;
    for (size_t i = 0; i < list_len; i++) {
      component_tuple2_u32_list_u8_free(&list_ptr[i]);
    }
    free(list_ptr);
  }
}

void component_option_u32_free(component_option_u32_t *ptr) {
  if (ptr->is_some) {
  }
}

void component_tuple2_option_u32_list_u8_free(component_tuple2_option_u32_list_u8_t *ptr) {
  component_option_u32_free(&ptr->f0);
  component_list_u8_free(&ptr->f1);
}

void component_list_tuple2_option_u32_list_u8_free(component_list_tuple2_option_u32_list_u8_t *ptr) {
  size_t list_len = ptr->len;
  if (list_len > 0) {
    component_tuple2_option_u32_list_u8_t *list_ptr = ptr->ptr;
    for (size_t i = 0; i < list_len; i++) {
      component_tuple2_option_u32_list_u8_free(&list_ptr[i]);
    }
    free(list_ptr);
  }
}

void component_list_list_u8_free(component_list_list_u8_t *ptr) {
  size_t list_len = ptr->len;
  if (list_len > 0) {
    component_list_u8_t *list_ptr = ptr->ptr;
    for (size_t i = 0; i < list_len; i++) {
      component_list_u8_free(&list_ptr[i]);
    }
    free(list_ptr);
  }
}

void component_list_string_free(component_list_string_t *ptr) {
  size_t list_len = ptr->len;
  if (list_len > 0) {
    component_string_t *list_ptr = ptr->ptr;
    for (size_t i = 0; i < list_len; i++) {
      component_string_free(&list_ptr[i]);
    }
    free(list_ptr);
  }
}

void wasmcloud_postgres_types_list_mac_address_eui48_free(wasmcloud_postgres_types_list_mac_address_eui48_t *ptr) {
  size_t list_len = ptr->len;
  if (list_len > 0) {
    wasmcloud_postgres_types_mac_address_eui48_t *list_ptr = ptr->ptr;
    for (size_t i = 0; i < list_len; i++) {
    }
    free(list_ptr);
  }
}

void wasmcloud_postgres_types_list_mac_address_eui64_free(wasmcloud_postgres_types_list_mac_address_eui64_t *ptr) {
  size_t list_len = ptr->len;
  if (list_len > 0) {
    wasmcloud_postgres_types_mac_address_eui64_t *list_ptr = ptr->ptr;
    for (size_t i = 0; i < list_len; i++) {
    }
    free(list_ptr);
  }
}

void component_list_tuple2_lower_left_point_upper_right_point_free(component_list_tuple2_lower_left_point_upper_right_point_t *ptr) {
  size_t list_len = ptr->len;
  if (list_len > 0) {
    component_tuple2_lower_left_point_upper_right_point_t *list_ptr = ptr->ptr;
    for (size_t i = 0; i < list_len; i++) {
    }
    free(list_ptr);
  }
}

void component_list_tuple2_center_point_radius_free(component_list_tuple2_center_point_radius_t *ptr) {
  size_t list_len = ptr->len;
  if (list_len > 0) {
    component_tuple2_center_point_radius_t *list_ptr = ptr->ptr;
    for (size_t i = 0; i < list_len; i++) {
    }
    free(list_ptr);
  }
}

void component_list_tuple2_start_point_end_point_free(component_list_tuple2_start_point_end_point_t *ptr) {
  size_t list_len = ptr->len;
  if (list_len > 0) {
    component_tuple2_start_point_end_point_t *list_ptr = ptr->ptr;
    for (size_t i = 0; i < list_len; i++) {
    }
    free(list_ptr);
  }
}

void component_list_point_free(component_list_point_t *ptr) {
  size_t list_len = ptr->len;
  if (list_len > 0) {
    wasmcloud_postgres_types_point_t *list_ptr = ptr->ptr;
    for (size_t i = 0; i < list_len; i++) {
    }
    free(list_ptr);
  }
}

void component_list_list_point_free(component_list_list_point_t *ptr) {
  size_t list_len = ptr->len;
  if (list_len > 0) {
    component_list_point_t *list_ptr = ptr->ptr;
    for (size_t i = 0; i < list_len; i++) {
      component_list_point_free(&list_ptr[i]);
    }
    free(list_ptr);
  }
}

void wasmcloud_postgres_types_list_date_free(wasmcloud_postgres_types_list_date_t *ptr) {
  size_t list_len = ptr->len;
  if (list_len > 0) {
    wasmcloud_postgres_types_date_t *list_ptr = ptr->ptr;
    for (size_t i = 0; i < list_len; i++) {
      wasmcloud_postgres_types_date_free(&list_ptr[i]);
    }
    free(list_ptr);
  }
}

void wasmcloud_postgres_types_list_interval_free(wasmcloud_postgres_types_list_interval_t *ptr) {
  size_t list_len = ptr->len;
  if (list_len > 0) {
    wasmcloud_postgres_types_interval_t *list_ptr = ptr->ptr;
    for (size_t i = 0; i < list_len; i++) {
      wasmcloud_postgres_types_interval_free(&list_ptr[i]);
    }
    free(list_ptr);
  }
}

void wasmcloud_postgres_types_list_time_free(wasmcloud_postgres_types_list_time_t *ptr) {
  size_t list_len = ptr->len;
  if (list_len > 0) {
    wasmcloud_postgres_types_time_t *list_ptr = ptr->ptr;
    for (size_t i = 0; i < list_len; i++) {
    }
    free(list_ptr);
  }
}

void wasmcloud_postgres_types_list_time_tz_free(wasmcloud_postgres_types_list_time_tz_t *ptr) {
  size_t list_len = ptr->len;
  if (list_len > 0) {
    wasmcloud_postgres_types_time_tz_t *list_ptr = ptr->ptr;
    for (size_t i = 0; i < list_len; i++) {
      wasmcloud_postgres_types_time_tz_free(&list_ptr[i]);
    }
    free(list_ptr);
  }
}

void wasmcloud_postgres_types_list_timestamp_free(wasmcloud_postgres_types_list_timestamp_t *ptr) {
  size_t list_len = ptr->len;
  if (list_len > 0) {
    wasmcloud_postgres_types_timestamp_t *list_ptr = ptr->ptr;
    for (size_t i = 0; i < list_len; i++) {
      wasmcloud_postgres_types_timestamp_free(&list_ptr[i]);
    }
    free(list_ptr);
  }
}

void wasmcloud_postgres_types_list_timestamp_tz_free(wasmcloud_postgres_types_list_timestamp_tz_t *ptr) {
  size_t list_len = ptr->len;
  if (list_len > 0) {
    wasmcloud_postgres_types_timestamp_tz_t *list_ptr = ptr->ptr;
    for (size_t i = 0; i < list_len; i++) {
      wasmcloud_postgres_types_timestamp_tz_free(&list_ptr[i]);
    }
    free(list_ptr);
  }
}

void component_list_u64_free(component_list_u64_t *ptr) {
  size_t list_len = ptr->len;
  if (list_len > 0) {
    uint64_t *list_ptr = ptr->ptr;
    for (size_t i = 0; i < list_len; i++) {
    }
    free(list_ptr);
  }
}

void component_tuple3_xmin_xmax_xip_list_free(component_tuple3_xmin_xmax_xip_list_t *ptr) {
  wasmcloud_postgres_types_xip_list_free(&ptr->f2);
}

void wasmcloud_postgres_types_list_lexeme_free(wasmcloud_postgres_types_list_lexeme_t *ptr) {
  size_t list_len = ptr->len;
  if (list_len > 0) {
    wasmcloud_postgres_types_lexeme_t *list_ptr = ptr->ptr;
    for (size_t i = 0; i < list_len; i++) {
      wasmcloud_postgres_types_lexeme_free(&list_ptr[i]);
    }
    free(list_ptr);
  }
}

void component_option_string_free(component_option_string_t *ptr) {
  if (ptr->is_some) {
    component_string_free(&ptr->val);
  }
}

void component_tuple2_string_option_string_free(component_tuple2_string_option_string_t *ptr) {
  component_string_free(&ptr->f0);
  component_option_string_free(&ptr->f1);
}

void component_list_tuple2_string_option_string_free(component_list_tuple2_string_option_string_t *ptr) {
  size_t list_len = ptr->len;
  if (list_len > 0) {
    component_tuple2_string_option_string_t *list_ptr = ptr->ptr;
    for (size_t i = 0; i < list_len; i++) {
      component_tuple2_string_option_string_free(&list_ptr[i]);
    }
    free(list_ptr);
  }
}

void wasmcloud_postgres_types_pg_value_free(wasmcloud_postgres_types_pg_value_t *ptr) {
  switch ((int32_t) ptr->tag) {
    case 1: {
      break;
    }
    case 2: {
      break;
    }
    case 3: {
      component_list_s64_free(&ptr->val.int8_array);
      break;
    }
    case 4: {
      break;
    }
    case 5: {
      break;
    }
    case 6: {
      break;
    }
    case 7: {
      break;
    }
    case 8: {
      component_list_bool_free(&ptr->val.bool_array);
      break;
    }
    case 9: {
      break;
    }
    case 10: {
      break;
    }
    case 11: {
      component_list_hashable_f64_free(&ptr->val.float8_array);
      break;
    }
    case 12: {
      break;
    }
    case 13: {
      break;
    }
    case 14: {
      component_list_hashable_f32_free(&ptr->val.float4_array);
      break;
    }
    case 15: {
      break;
    }
    case 16: {
      break;
    }
    case 17: {
      break;
    }
    case 18: {
      component_list_s32_free(&ptr->val.int4_array);
      break;
    }
    case 19: {
      wasmcloud_postgres_types_numeric_free(&ptr->val.numeric);
      break;
    }
    case 20: {
      wasmcloud_postgres_types_numeric_free(&ptr->val.decimal);
      break;
    }
    case 21: {
      component_list_numeric_free(&ptr->val.numeric_array);
      break;
    }
    case 22: {
      break;
    }
    case 23: {
      break;
    }
    case 24: {
      break;
    }
    case 25: {
      break;
    }
    case 26: {
      component_list_s16_free(&ptr->val.int2_array);
      break;
    }
    case 27: {
      component_list_s16_free(&ptr->val.int2_vector);
      break;
    }
    case 28: {
      component_list_list_s16_free(&ptr->val.int2_vector_array);
      break;
    }
    case 29: {
      break;
    }
    case 30: {
      break;
    }
    case 31: {
      component_tuple2_u32_list_u8_free(&ptr->val.bit);
      break;
    }
    case 32: {
      component_list_tuple2_u32_list_u8_free(&ptr->val.bit_array);
      break;
    }
    case 33: {
      component_tuple2_option_u32_list_u8_free(&ptr->val.bit_varying);
      break;
    }
    case 34: {
      component_tuple2_option_u32_list_u8_free(&ptr->val.varbit);
      break;
    }
    case 35: {
      component_list_tuple2_option_u32_list_u8_free(&ptr->val.varbit_array);
      break;
    }
    case 36: {
      component_list_u8_free(&ptr->val.bytea);
      break;
    }
    case 37: {
      component_list_list_u8_free(&ptr->val.bytea_array);
      break;
    }
    case 38: {
      component_tuple2_u32_list_u8_free(&ptr->val.char_);
      break;
    }
    case 39: {
      component_list_tuple2_u32_list_u8_free(&ptr->val.char_array);
      break;
    }
    case 40: {
      component_tuple2_option_u32_list_u8_free(&ptr->val.varchar);
      break;
    }
    case 41: {
      component_list_tuple2_option_u32_list_u8_free(&ptr->val.varchar_array);
      break;
    }
    case 42: {
      component_string_free(&ptr->val.cidr);
      break;
    }
    case 43: {
      component_list_string_free(&ptr->val.cidr_array);
      break;
    }
    case 44: {
      component_string_free(&ptr->val.inet);
      break;
    }
    case 45: {
      component_list_string_free(&ptr->val.inet_array);
      break;
    }
    case 46: {
      break;
    }
    case 47: {
      wasmcloud_postgres_types_list_mac_address_eui48_free(&ptr->val.macaddr_array);
      break;
    }
    case 48: {
      break;
    }
    case 49: {
      wasmcloud_postgres_types_list_mac_address_eui64_free(&ptr->val.macaddr8_array);
      break;
    }
    case 50: {
      break;
    }
    case 51: {
      component_list_tuple2_lower_left_point_upper_right_point_free(&ptr->val.box_array);
      break;
    }
    case 52: {
      break;
    }
    case 53: {
      component_list_tuple2_center_point_radius_free(&ptr->val.circle_array);
      break;
    }
    case 54: {
      break;
    }
    case 55: {
      component_list_tuple2_start_point_end_point_free(&ptr->val.line_array);
      break;
    }
    case 56: {
      break;
    }
    case 57: {
      component_list_tuple2_start_point_end_point_free(&ptr->val.lseg_array);
      break;
    }
    case 58: {
      component_list_point_free(&ptr->val.path);
      break;
    }
    case 59: {
      component_list_list_point_free(&ptr->val.path_array);
      break;
    }
    case 60: {
      break;
    }
    case 61: {
      component_list_point_free(&ptr->val.point_array);
      break;
    }
    case 62: {
      component_list_point_free(&ptr->val.polygon);
      break;
    }
    case 63: {
      component_list_list_point_free(&ptr->val.polygon_array);
      break;
    }
    case 64: {
      wasmcloud_postgres_types_date_free(&ptr->val.date);
      break;
    }
    case 65: {
      wasmcloud_postgres_types_list_date_free(&ptr->val.date_array);
      break;
    }
    case 66: {
      wasmcloud_postgres_types_interval_free(&ptr->val.interval);
      break;
    }
    case 67: {
      wasmcloud_postgres_types_list_interval_free(&ptr->val.interval_array);
      break;
    }
    case 68: {
      break;
    }
    case 69: {
      wasmcloud_postgres_types_list_time_free(&ptr->val.time_array);
      break;
    }
    case 70: {
      wasmcloud_postgres_types_time_tz_free(&ptr->val.time_tz);
      break;
    }
    case 71: {
      wasmcloud_postgres_types_list_time_tz_free(&ptr->val.time_tz_array);
      break;
    }
    case 72: {
      wasmcloud_postgres_types_timestamp_free(&ptr->val.timestamp);
      break;
    }
    case 73: {
      wasmcloud_postgres_types_list_timestamp_free(&ptr->val.timestamp_array);
      break;
    }
    case 74: {
      wasmcloud_postgres_types_timestamp_tz_free(&ptr->val.timestamp_tz);
      break;
    }
    case 75: {
      wasmcloud_postgres_types_list_timestamp_tz_free(&ptr->val.timestamp_tz_array);
      break;
    }
    case 76: {
      component_string_free(&ptr->val.json);
      break;
    }
    case 77: {
      component_list_string_free(&ptr->val.json_array);
      break;
    }
    case 78: {
      component_string_free(&ptr->val.jsonb);
      break;
    }
    case 79: {
      component_list_string_free(&ptr->val.jsonb_array);
      break;
    }
    case 80: {
      wasmcloud_postgres_types_numeric_free(&ptr->val.money);
      break;
    }
    case 81: {
      component_list_numeric_free(&ptr->val.money_array);
      break;
    }
    case 82: {
      break;
    }
    case 83: {
      component_list_u64_free(&ptr->val.pg_lsn_array);
      break;
    }
    case 84: {
      component_tuple3_xmin_xmax_xip_list_free(&ptr->val.pg_snapshot);
      break;
    }
    case 85: {
      break;
    }
    case 86: {
      component_string_free(&ptr->val.name);
      break;
    }
    case 87: {
      component_list_string_free(&ptr->val.name_array);
      break;
    }
    case 88: {
      component_string_free(&ptr->val.text);
      break;
    }
    case 89: {
      component_list_string_free(&ptr->val.text_array);
      break;
    }
    case 90: {
      component_string_free(&ptr->val.xml);
      break;
    }
    case 91: {
      component_list_string_free(&ptr->val.xml_array);
      break;
    }
    case 92: {
      component_string_free(&ptr->val.ts_query);
      break;
    }
    case 93: {
      wasmcloud_postgres_types_list_lexeme_free(&ptr->val.ts_vector);
      break;
    }
    case 94: {
      component_string_free(&ptr->val.uuid);
      break;
    }
    case 95: {
      component_list_string_free(&ptr->val.uuid_array);
      break;
    }
    case 96: {
      component_list_tuple2_string_option_string_free(&ptr->val.hstore);
      break;
    }
  }
}

void wasmcloud_postgres_types_result_row_entry_free(wasmcloud_postgres_types_result_row_entry_t *ptr) {
  component_string_free(&ptr->column_name);
  wasmcloud_postgres_types_pg_value_free(&ptr->value);
}

void wasmcloud_postgres_types_result_row_free(wasmcloud_postgres_types_result_row_t *ptr) {
  size_t list_len = ptr->len;
  if (list_len > 0) {
    wasmcloud_postgres_types_result_row_entry_t *list_ptr = ptr->ptr;
    for (size_t i = 0; i < list_len; i++) {
      wasmcloud_postgres_types_result_row_entry_free(&list_ptr[i]);
    }
    free(list_ptr);
  }
}

void wasmcloud_postgres_query_pg_value_free(wasmcloud_postgres_query_pg_value_t *ptr) {
  wasmcloud_postgres_types_pg_value_free(ptr);
}

void wasmcloud_postgres_query_result_row_free(wasmcloud_postgres_query_result_row_t *ptr) {
  wasmcloud_postgres_types_result_row_free(ptr);
}

void wasmcloud_postgres_query_query_error_free(wasmcloud_postgres_query_query_error_t *ptr) {
  wasmcloud_postgres_types_query_error_free(ptr);
}

void wasmcloud_postgres_query_list_pg_value_free(wasmcloud_postgres_query_list_pg_value_t *ptr) {
  size_t list_len = ptr->len;
  if (list_len > 0) {
    wasmcloud_postgres_query_pg_value_t *list_ptr = ptr->ptr;
    for (size_t i = 0; i < list_len; i++) {
      wasmcloud_postgres_query_pg_value_free(&list_ptr[i]);
    }
    free(list_ptr);
  }
}

void wasmcloud_postgres_query_list_result_row_free(wasmcloud_postgres_query_list_result_row_t *ptr) {
  size_t list_len = ptr->len;
  if (list_len > 0) {
    wasmcloud_postgres_query_result_row_t *list_ptr = ptr->ptr;
    for (size_t i = 0; i < list_len; i++) {
      wasmcloud_postgres_query_result_row_free(&list_ptr[i]);
    }
    free(list_ptr);
  }
}

void wasmcloud_postgres_query_result_list_result_row_query_error_free(wasmcloud_postgres_query_result_list_result_row_query_error_t *ptr) {
  if (!ptr->is_err) {
    wasmcloud_postgres_query_list_result_row_free(&ptr->val.ok);
  } else {
    wasmcloud_postgres_query_query_error_free(&ptr->val.err);
  }
}

void wasmcloud_postgres_query_result_void_query_error_free(wasmcloud_postgres_query_result_void_query_error_t *ptr) {
  if (!ptr->is_err) {
  } else {
    wasmcloud_postgres_query_query_error_free(&ptr->val.err);
  }
}

void component_string_set(component_string_t *ret, const char*s) {
  ret->ptr = (uint8_t*) s;
  ret->len = strlen(s);
}

void component_string_dup(component_string_t *ret, const char*s) {
  ret->len = strlen(s);
  ret->ptr = (uint8_t*) cabi_realloc(NULL, 0, 1, ret->len * 1);
  memcpy(ret->ptr, s, ret->len * 1);
}

void component_string_free(component_string_t *ret) {
  if (ret->len > 0) {
    free(ret->ptr);
  }
  ret->ptr = NULL;
  ret->len = 0;
}

// Component Adapters

__attribute__((__aligned__(4)))
static uint8_t RET_AREA[8];

void wasmcloud_postgres_query_query(component_string_t *query, wasmcloud_postgres_query_list_pg_value_t *params, wasmcloud_postgres_query_result_list_result_row_query_error_t *ret) {
  __attribute__((__aligned__(4)))
  uint8_t ret_area[16];
  uint8_t *ptr = (uint8_t *) &ret_area;
  __wasm_import_wasmcloud_postgres_query_query((uint8_t *) (*query).ptr, (*query).len, (uint8_t *) (*params).ptr, (*params).len, ptr);
  wasmcloud_postgres_query_result_list_result_row_query_error_t result;
  switch ((int32_t) *((uint8_t*) (ptr + 0))) {
    case 0: {
      result.is_err = false;
      result.val.ok = (wasmcloud_postgres_query_list_result_row_t) { (wasmcloud_postgres_query_result_row_t*)(*((uint8_t **) (ptr + 4))), (*((size_t*) (ptr + 8))) };
      break;
    }
    case 1: {
      result.is_err = true;
      wasmcloud_postgres_types_query_error_t variant165;
      variant165.tag = (int32_t) *((uint8_t*) (ptr + 4));
      switch ((int32_t) variant165.tag) {
        case 0: {
          variant165.val.invalid_params = (component_string_t) { (uint8_t*)(*((uint8_t **) (ptr + 8))), (*((size_t*) (ptr + 12))) };
          break;
        }
        case 1: {
          variant165.val.invalid_query = (component_string_t) { (uint8_t*)(*((uint8_t **) (ptr + 8))), (*((size_t*) (ptr + 12))) };
          break;
        }
        case 2: {
          variant165.val.unexpected = (component_string_t) { (uint8_t*)(*((uint8_t **) (ptr + 8))), (*((size_t*) (ptr + 12))) };
          break;
        }
      }

      result.val.err = variant165;
      break;
    }
  }
  *ret = result;
}

void wasmcloud_postgres_query_query_batch(component_string_t *query, wasmcloud_postgres_query_result_void_query_error_t *ret) {
  __attribute__((__aligned__(4)))
  uint8_t ret_area[16];
  uint8_t *ptr = (uint8_t *) &ret_area;
  __wasm_import_wasmcloud_postgres_query_query_batch((uint8_t *) (*query).ptr, (*query).len, ptr);
  wasmcloud_postgres_query_result_void_query_error_t result;
  switch ((int32_t) *((uint8_t*) (ptr + 0))) {
    case 0: {
      result.is_err = false;
      break;
    }
    case 1: {
      result.is_err = true;
      wasmcloud_postgres_types_query_error_t variant;
      variant.tag = (int32_t) *((uint8_t*) (ptr + 4));
      switch ((int32_t) variant.tag) {
        case 0: {
          variant.val.invalid_params = (component_string_t) { (uint8_t*)(*((uint8_t **) (ptr + 8))), (*((size_t*) (ptr + 12))) };
          break;
        }
        case 1: {
          variant.val.invalid_query = (component_string_t) { (uint8_t*)(*((uint8_t **) (ptr + 8))), (*((size_t*) (ptr + 12))) };
          break;
        }
        case 2: {
          variant.val.unexpected = (component_string_t) { (uint8_t*)(*((uint8_t **) (ptr + 8))), (*((size_t*) (ptr + 12))) };
          break;
        }
      }

      result.val.err = variant;
      break;
    }
  }
  *ret = result;
}

__attribute__((__export_name__("wasmcloud:examples/invoke#call")))
uint8_t * __wasm_export_exports_wasmcloud_examples_invoke_call(void) {
  component_string_t ret;
  exports_wasmcloud_examples_invoke_call(&ret);
  uint8_t *ptr = (uint8_t *) &RET_AREA;
  *((size_t*)(ptr + 4)) = (ret).len;
  *((uint8_t **)(ptr + 0)) = (uint8_t *) (ret).ptr;
  return ptr;
}

// Ensure that the *_component_type.o object is linked in

extern void __component_type_object_force_link_component(void);
void __component_type_object_force_link_component_public_use_in_this_compilation_unit(void) {
  __component_type_object_force_link_component();
}
