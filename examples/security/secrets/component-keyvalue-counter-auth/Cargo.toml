[package]
name = "component-keyvalue-counter-auth"
edition = "2021"
version = "0.1.0"
description = "A component that provides a key-value store counter over HTTP with authentication."

[workspace]

[lib]
crate-type = ["cdylib"]

[dependencies]
wit-bindgen = { version = "0.24", features = ["default"] }
