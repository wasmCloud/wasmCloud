# `qrcode`

Generate QR Codes with Wasm.

# Quickstart

To start a local development loop with [Wasm Shell](https://github.com/wasmCloud/wasmCloud):

```shell
wash dev
```

Then navigate to [http://localhost:8000](http://localhost:8000)

To build the component:

```shell
wash build
```
