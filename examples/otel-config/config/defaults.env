# App defaults loaded by `wash dev` via `configs.app-defaults` in
# .wash/config.yaml. Lines are KEY=value, `#` starts a comment.

LOG_LEVEL=info
SERVICE_NAME=otel-config-dev
OUTBOUND_HOST=example.com
request_timeout_ms=5000
