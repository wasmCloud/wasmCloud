[package]
name = "cron-service"
version = "0.1.0"
edition = "2024"

[dependencies]
wit-bindgen = { workspace = true }
tokio = { workspace = true, default-features = false, features = ["sync", "macros", "io-util", "rt", "time"] }
