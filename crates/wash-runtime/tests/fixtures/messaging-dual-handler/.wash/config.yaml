build:
  # wit/deps is staged by xtask (see wit/world.wit), which builds with --skip-fetch.
  command: cargo build --target wasm32-wasip1 --release
  component_path: ../target/wasm32-wasip1/release/messaging_dual_handler.wasm
