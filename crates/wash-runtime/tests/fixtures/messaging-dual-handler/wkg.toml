[overrides]
"wasi:cli" = { path = "../p3-wit-deps/wasi-cli-0.3.0" }
"wasi:clocks" = { path = "../p3-wit-deps/wasi-clocks-0.3.0" }
"wasi:filesystem" = { path = "../p3-wit-deps/wasi-filesystem-0.3.0" }
"wasi:random" = { path = "../p3-wit-deps/wasi-random-0.3.0" }
"wasi:sockets" = { path = "../p3-wit-deps/wasi-sockets-0.3.0" }
# Both revisions this fixture exports arrive through this one override: an
# override maps a package NAME to a single directory, and that directory holds
# `@0.3.0` with `@0.2.0` nested beside it.
"wasmcloud:messaging" = { path = "../p3-wit-deps/wasmcloud-messaging-dual" }
