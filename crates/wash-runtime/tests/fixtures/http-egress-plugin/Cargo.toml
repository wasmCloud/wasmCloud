[package]
name = "http-egress-plugin"
edition = "2021"
version = "0.0.0"

[lib]
crate-type = ["cdylib"]

[dependencies]
anyhow = { workspace = true }
wit-bindgen = { workspace = true, features = ["async-spawn", "inter-task-wakeup"] }
