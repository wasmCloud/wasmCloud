kind: ConfigMap
apiVersion: v1
metadata:
  name: {{ include "benchmark.fullname" . }}-grafana-dashboards
  labels:
    {{- include "benchmark.labels" . | nindent 4 }}
data:
{{ (.Files.Glob "dashboards/*").AsConfig | indent 2 }}
  